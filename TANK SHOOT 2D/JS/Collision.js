// Definicion de la clase que representara el choque en el juego 
class Collision{
    // Declaración de las propiedades X y Y para almacenar las coordenadas
    // Asignar la duracion de la animacion 
    posX;
    posY;
    duracion;
// Constructor de la clase, que se ejecuta al crear una nueva instancia
    constructor(_posX, _posY, _duracion){
        //Asignar valores para cada parametro 
        this.posX = _posX;
        this.posY = _posY;
        this.duracion = _duracion;

    }

// Metodo para llamar a la animacion 
    playAnimation(){
        
    }

}